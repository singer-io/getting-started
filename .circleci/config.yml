version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - run:
          name: 'Setup environment'
          command: |
            apt update
            apt install --yes make git
      - checkout
      - run:
          name: 'Tests'
          command: |
            make
